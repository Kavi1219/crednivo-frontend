CREDNIVO PHASE 5.0.2 - PROFILE MODAL + REPORT THEME FIX

Fixed:
- Edit Company Profile now renders through a React portal to document.body.
- Edit Profile is no longer clipped by the sticky/blurred header on desktop.
- Edit Profile stays inside the viewport and scrolls safely when needed.
- Mobile Edit Profile opens as a bottom sheet with accessible header/actions.
- Escape key and backdrop close are preserved; background scrolling is locked.
- Change Password modal uses the same safe portal behavior.
- Monthly Business Statement header now uses shared theme surfaces directly.
- Dark theme no longer produces a white statement header with invisible text.
- Existing Phase 5.0 report logic and backend API usage are unchanged.
